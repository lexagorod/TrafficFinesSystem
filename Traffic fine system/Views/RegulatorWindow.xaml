<Window x:Class="TrafficFineSystem.RegulatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TrafficFineSystem"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="RegulatorWindow" Height="450" Width="800">
    <Grid>

        <Grid>

             <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
             </Grid.ColumnDefinitions>

             <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <DataGrid x:Name="FinesInfo" Grid.Row="0">

            </DataGrid>

            <Button Name="SaveButton"  Grid.Row="1" HorizontalAlignment="Left" Content="Сохранить" ClickMode="Release" Width="70" Height="50">
             </Button>

            <TextBlock x:Name="changeSuccess"  Grid.Row="2" HorizontalAlignment="Left" FontSize="18" Margin="10"/>
         </Grid>

        </Grid>
</Window>
